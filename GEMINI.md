# Claude Usage Tray Windows — Agent Instructions

이 문서는 Gemini CLI 에이전트가 이 프로젝트에서 작업할 때 준수해야 할 **최우선 지침**입니다.

---

## 🚀 로컬 빌드 및 실행 금지

이 프로젝트는 **GitHub Actions(CI/CD)를 통해 빌드 및 배포**되도록 설정되어 있습니다. 따라서 에이전트는 로컬 환경에서 다음과 같은 작업을 수행하지 않습니다.

1.  **로컬 빌드 금지**: `dotnet build`, `msbuild` 등을 로컬에서 실행할 필요가 없습니다.
2.  **로컬 실행 금지**: `dotnet run` 등을 통해 앱을 로컬에서 실행하지 않습니다.
3.  **검증 방식**: 코드의 정확성은 정적 분석, 단위 테스트(`ClaudeUsageTray.Tests`), 그리고 GitHub Actions의 빌드 결과를 통해 확인합니다.
4.  **릴리즈**: 버전 업데이트는 `release.bat`을 사용하거나 수동으로 `csproj` 수정 후 태그를 푸시하여 GitHub Actions가 처리하도록 합니다.

---

## 🛠️ 작업 워크플로우

0.  **Source Sync First**: 세션을 시작하거나 첫 요청을 받았을 때, 항상 로컬 소스 코드가 최신인지 확인합니다 (`git fetch && git status`). 최신이 아닐 경우 `git pull`을 통해 동기화한 후 작업을 시작합니다.
1.  **변경 사항 적용**: 코드 수정 및 테스트 코드 작성.
2.  **테스트 실행**: 로컬에서 `dotnet test`를 실행하여 로직 검증 (빌드 도구가 필요하지만, 이는 테스트 실행을 위한 최소한의 과정으로 간주합니다).
3.  **커밋 및 푸시**: 변경 사항을 커밋하고 푸시합니다.
4.  **CI 확인**: GitHub Actions 탭에서 빌드 성공 여부를 확인합니다.

---

## 📝 문서 관리

- **CHANGELOG.md**: 모든 기능 추가, 버그 수정, 개선 사항은 반드시 기록합니다.
- **AGENTS.md / MEMORY.md**: 작업 이력 및 레슨 런을 지속적으로 업데이트합니다.
