<Window x:Class="ClaudeUsageTray.Updater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Claude Usage Tray 업데이트"
        Width="400" Height="220"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E2E">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Claude Usage Tray 업데이트"
                   FontSize="18" FontWeight="SemiBold"
                   Foreground="#CDD6F4"
                   HorizontalAlignment="Center"/>

        <!-- Status Message -->
        <TextBlock x:Name="StatusText"
                   Grid.Row="2"
                   Text="대기 중..."
                   FontSize="14"
                   Foreground="#89B4FA"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   TextAlignment="Center"/>

        <!-- Progress Bar -->
        <Border Grid.Row="4" CornerRadius="4" Background="#313244">
            <Grid>
                <ProgressBar x:Name="ProgressBar"
                             Height="8"
                             Minimum="0" Maximum="100"
                             Value="0"
                             Background="#313244"
                             Foreground="#89B4FA"
                             BorderThickness="0"/>
            </Grid>
        </Border>

        <!-- Percentage -->
        <TextBlock x:Name="PercentText"
                   Grid.Row="6"
                   Text="0%"
                   FontSize="12"
                   Foreground="#A6ADC8"
                   HorizontalAlignment="Center"
                   Margin="0,8,0,0"/>
    </Grid>
</Window>