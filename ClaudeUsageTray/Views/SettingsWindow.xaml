<Window x:Class="ClaudeUsageTray.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Width="320"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="BgBrush"     Color="#0F1117"/>
        <SolidColorBrush x:Key="CardBrush"   Color="#1A1D2E"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#2D2F45"/>

        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#8B92B3"/>
        </Style>

        <Style x:Key="CheckStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#C4C9E0"/>
            <Setter Property="FontSize"   Value="12"/>
            <Setter Property="Margin"     Value="0,0,0,6"/>
        </Style>

        <Style x:Key="DarkCombo" TargetType="ComboBox">
            <Setter Property="Background"         Value="#1A1D2E"/>
            <Setter Property="Foreground"         Value="#F1F5F9"/>
            <Setter Property="BorderBrush"        Value="#2D2F45"/>
            <Setter Property="BorderThickness"    Value="1"/>
            <Setter Property="FontSize"           Value="12"/>
            <Setter Property="Height"             Value="32"/>
            <Setter Property="Padding"            Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="28"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="▾"
                                               Foreground="#8B92B3" FontSize="10"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False">
                                <Border Background="#1A1D2E" BorderBrush="#2D2F45" BorderThickness="1"
                                        CornerRadius="8" Margin="0,2,0,0"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.4" Color="#000000"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="200">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                            <ToggleButton Background="Transparent" BorderThickness="0"
                                          Focusable="False" IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#8B5CF6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#5A5F7A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="#C4C9E0"/>
                        <Setter Property="FontSize"   Value="12"/>
                        <Setter Property="Padding"    Value="10,7"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="ItemBorder" Background="Transparent"
                                            CornerRadius="6" Margin="4,2">
                                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#2D2F45"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Foreground" Value="#A78BFA"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkBtn" TargetType="Button">
            <Setter Property="Background"       Value="Transparent"/>
            <Setter Property="BorderThickness"  Value="0"/>
            <Setter Property="Cursor"           Value="Hand"/>
            <Setter Property="Foreground"       Value="#A78BFA"/>
            <Setter Property="FontSize"         Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}"
                                   Foreground="{TemplateBinding Foreground}"
                                   FontSize="{TemplateBinding FontSize}"
                                   TextDecorations="Underline"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.5" Color="#000000"/>
        </Border.Effect>
        <Border Background="{StaticResource BgBrush}"
                CornerRadius="16"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1">
            <StackPanel>

                <!-- Header -->
                <Border Background="#1A1D2E" CornerRadius="16,16,0,0" Padding="16,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⚙  " Foreground="#A78BFA" FontSize="14"/>
                            <TextBlock x:Name="TitleText" Foreground="#F1F5F9"
                                       FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        <Button Grid.Column="1" Width="28" Height="28"
                                Background="Transparent" BorderThickness="0"
                                Cursor="Hand" Click="CloseBtn_Click">
                            <TextBlock Text="✕" Foreground="#8B92B3" FontSize="12"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#2D2F45"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>

                <!-- Content -->
                <StackPanel Margin="16,14,16,0">

                    <!-- General -->
                    <TextBlock x:Name="LblGeneral" Style="{StaticResource SectionLabel}" Margin="0,0,0,10"/>
                    <CheckBox x:Name="ChkEnabled"          Style="{StaticResource CheckStyle}" Click="Setting_Changed"/>
                    <CheckBox x:Name="ChkRateLimit"        Style="{StaticResource CheckStyle}" Click="Setting_Changed"/>
                    <CheckBox x:Name="ChkQuotaReset"       Style="{StaticResource CheckStyle}" Click="Setting_Changed"/>
                    <CheckBox x:Name="ChkStartWithWindows" Style="{StaticResource CheckStyle}" Click="StartWithWindows_Changed"/>

                    <!-- Polling Interval -->
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,4">
                        <TextBlock x:Name="LblPollingInterval" Foreground="#C4C9E0" FontSize="12" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtPollingValue" Foreground="#A78BFA" FontSize="12" FontWeight="SemiBold"
                                   VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="1" Foreground="#5A5F7A" FontSize="10" VerticalAlignment="Center"/>
                        <Slider x:Name="SliderPolling" Grid.Column="1"
                                Minimum="1" Maximum="10" Value="2"
                                TickFrequency="1" IsSnapToTickEnabled="True"
                                Margin="8,0"
                                ValueChanged="SliderPolling_ValueChanged">
                            <Slider.Style>
                                <Style TargetType="Slider">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Slider">
                                                <Grid>
                                                    <Border Background="#2D2F45" CornerRadius="3" Height="6"/>
                                                    <Track x:Name="PART_Track">
                                                        <Track.Thumb>
                                                            <Thumb>
                                                                <Thumb.Style>
                                                                    <Style TargetType="Thumb">
                                                                        <Setter Property="Width" Value="16"/>
                                                                        <Setter Property="Height" Value="16"/>
                                                                        <Setter Property="Template">
                                                                            <Setter.Value>
                                                                                <ControlTemplate TargetType="Thumb">
                                                                                    <Ellipse Fill="#8B5CF6" Width="16" Height="16"/>
                                                                                </ControlTemplate>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                    </Style>
                                                                </Thumb.Style>
                                                            </Thumb>
                                                        </Track.Thumb>
                                                    </Track>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Slider.Style>
                        </Slider>
                        <TextBlock Grid.Column="2" Text="10" Foreground="#5A5F7A" FontSize="10" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Language -->
                    <Border Height="1" Background="#2D2F45" Margin="0,8,0,14"/>
                    <TextBlock x:Name="LblLanguageSection" Style="{StaticResource SectionLabel}" Margin="0,0,0,8"/>
                    <ComboBox x:Name="CmbLanguage" Style="{StaticResource DarkCombo}"
                              Margin="0,0,0,14" SelectionChanged="CmbLanguage_SelectionChanged">
                        <ComboBoxItem x:Name="LangItemSystem"  Tag="system"/>
                        <ComboBoxItem Content="한국어"           Tag="ko"/>
                        <ComboBoxItem Content="English"        Tag="en"/>
                        <ComboBoxItem Content="中文"            Tag="zh"/>
                        <ComboBoxItem Content="日本語"          Tag="ja"/>
                    </ComboBox>

                    <Button x:Name="BtnTestNotification" Style="{StaticResource LinkBtn}"
                            Margin="0,2,0,2" Click="BtnTestNotification_Click"/>
                    <TextBlock x:Name="LblTestNotificationHint" Foreground="#5A5F7A" FontSize="10" Margin="0,0,0,6"/>

                    <!-- Thresholds -->
                    <Border Height="1" Background="#2D2F45" Margin="0,8,0,14"/>
                    <TextBlock x:Name="LblThresholds" Style="{StaticResource SectionLabel}" Margin="0,0,0,10"/>
                    <WrapPanel>
                        <CheckBox x:Name="Chk50"  Content="50%"  Style="{StaticResource CheckStyle}" Margin="0,0,14,6" Click="Setting_Changed"/>
                        <CheckBox x:Name="Chk75"  Content="75%"  Style="{StaticResource CheckStyle}" Margin="0,0,14,6" Click="Setting_Changed"/>
                        <CheckBox x:Name="Chk90"  Content="90%"  Style="{StaticResource CheckStyle}" Margin="0,0,14,6" Click="Setting_Changed"/>
                        <CheckBox x:Name="Chk100" Content="100%" Style="{StaticResource CheckStyle}" Margin="0,0,0,6"  Click="Setting_Changed"/>
                    </WrapPanel>

                    <!-- ntfy divider -->
                    <Border Height="1" Background="#2D2F45" Margin="0,8,0,14"/>

                    <!-- ntfy header -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="📱  " FontSize="13"/>
                        <TextBlock x:Name="LblNtfyTitle" Foreground="#F1F5F9"
                                   FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- ntfy description -->
                    <Border Background="#1A1D2E" CornerRadius="10" Padding="12,10" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock x:Name="LblNtfyDesc" Foreground="#8B92B3" FontSize="11"
                                       TextWrapping="Wrap" LineHeight="18" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                <TextBlock Text="① " Foreground="#8B92B3" FontSize="11"/>
                                <Button x:Name="BtnNtfyDownload" Style="{StaticResource LinkBtn}"
                                        Click="BtnNtfyDownload_Click"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                                <TextBlock x:Name="LblStep2" Foreground="#8B92B3" FontSize="11" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                <TextBlock x:Name="LblStep3" Foreground="#8B92B3" FontSize="11" TextWrapping="Wrap"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- ntfy topic input -->
                    <TextBlock x:Name="LblNtfyTopic" Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
                    <TextBox x:Name="TxtNtfyTopic"
                             Background="#1A1D2E" Foreground="#F1F5F9"
                             BorderBrush="#2D2F45" BorderThickness="1"
                             Padding="10,7" FontSize="12"
                             LostFocus="TxtNtfyTopic_LostFocus"
                             KeyDown="TxtNtfyTopic_KeyDown">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter Property="BorderBrush" Value="#8B5CF6"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <TextBlock x:Name="LblNtfySecurityWarning" Foreground="#FCD34D" FontSize="10"
                               TextWrapping="Wrap" Margin="2,4,0,2"
                               Visibility="Collapsed"/>
                    <TextBlock x:Name="LblNtfyHint" Foreground="#5A5F7A" FontSize="10" Margin="2,2,0,10"/>

                    <!-- ntfy 중복 발송 방지 -->
                    <CheckBox x:Name="ChkNtfySendFromThisPc" Style="{StaticResource CheckStyle}"
                              Margin="0,0,0,2" Click="Setting_Changed"/>
                    <TextBlock x:Name="LblNtfySendFromThisPcHint" Foreground="#5A5F7A" FontSize="10"
                               TextWrapping="Wrap" Margin="2,0,0,14"/>

                </StackPanel>

                <!-- Disclaimer -->
                <TextBlock x:Name="LblDisclaimer" Margin="16,4,16,14" TextWrapping="Wrap" FontSize="10" Foreground="#3D4266"/>

            </StackPanel>
        </Border>
    </Border>
</Window>
