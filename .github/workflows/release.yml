name: Build & Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-sc:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

      - name: Get version from tag
        id: version
        shell: bash
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Build self-contained release
        run: |
          dotnet publish ClaudeUsageTray/ClaudeUsageTray.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:EnableCompressionInSingleFile=true `
            -p:IncludeNativeLibrariesForSelfExtract=true `
            -p:PublishDir=bin/sc `
            -p:Version=${{ steps.version.outputs.VERSION }}

      - name: Generate SHA256
        shell: powershell
        run: |
          $hash = (Get-FileHash "ClaudeUsageTray/bin/sc/ClaudeUsageTray.exe" -Algorithm SHA256).Hash.ToLower()
          "$hash  ClaudeUsageTray-sc.exe" | Out-File -FilePath "ClaudeUsageTray-sc.sha256" -Encoding ASCII
          Write-Host "sc size: $([math]::Round((Get-Item 'ClaudeUsageTray/bin/sc/ClaudeUsageTray.exe').Length/1MB, 1)) MB"
          Write-Host "SHA256: $hash"

      - name: Rename executable
        shell: powershell
        run: Copy-Item "ClaudeUsageTray/bin/sc/ClaudeUsageTray.exe" "ClaudeUsageTray-sc.exe"

      - name: Upload sc artifact
        uses: actions/upload-artifact@v4
        with:
          name: sc-release
          path: |
            ClaudeUsageTray-sc.exe
            ClaudeUsageTray-sc.sha256

  build-fd:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

      - name: Get version from tag
        id: version
        shell: bash
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Build framework-dependent release
        run: |
          dotnet publish ClaudeUsageTray/ClaudeUsageTray.csproj `
            -c Release `
            -r win-x64 `
            --self-contained false `
            -p:PublishSingleFile=true `
            -p:PublishDir=bin/fd `
            -p:Version=${{ steps.version.outputs.VERSION }}

      - name: Generate SHA256
        shell: powershell
        run: |
          $hash = (Get-FileHash "ClaudeUsageTray/bin/fd/ClaudeUsageTray.exe" -Algorithm SHA256).Hash.ToLower()
          "$hash  ClaudeUsageTray-fd.exe" | Out-File -FilePath "ClaudeUsageTray-fd.sha256" -Encoding ASCII
          Write-Host "fd size: $([math]::Round((Get-Item 'ClaudeUsageTray/bin/fd/ClaudeUsageTray.exe').Length/1MB, 1)) MB"
          Write-Host "SHA256: $hash"

      - name: Rename executable
        shell: powershell
        run: Copy-Item "ClaudeUsageTray/bin/fd/ClaudeUsageTray.exe" "ClaudeUsageTray-fd.exe"

      - name: Upload fd artifact
        uses: actions/upload-artifact@v4
        with:
          name: fd-release
          path: |
            ClaudeUsageTray-fd.exe
            ClaudeUsageTray-fd.sha256

  release:
    needs: [build-sc, build-fd]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download sc artifact
        uses: actions/download-artifact@v4
        with:
          name: sc-release
          path: artifacts/

      - name: Download fd artifact
        uses: actions/download-artifact@v4
        with:
          name: fd-release
          path: artifacts/

      - name: Extract changelog for this version
        id: changelog
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          NOTES=$(awk "/^## \[$VERSION\]/{found=1; next} found && /^## \[/{exit} found{print}" CHANGELOG.md)
          echo "NOTES<<EOF" >> $GITHUB_OUTPUT
          echo "$NOTES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "v${GITHUB_REF_NAME#v}"
          body: ${{ steps.changelog.outputs.NOTES }}
          files: |
            artifacts/ClaudeUsageTray-sc.exe
            artifacts/ClaudeUsageTray-sc.sha256
            artifacts/ClaudeUsageTray-fd.exe
            artifacts/ClaudeUsageTray-fd.sha256
